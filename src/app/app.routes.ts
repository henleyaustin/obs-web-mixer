import { Routes } from '@angular/router';
import { SliderPageComponent } from './pages/slider-page/slider-page.component';

export const routes: Routes = [{ path: '*', component: SliderPageComponent }];
